import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // React compiler disabled to avoid requiring babel-plugin-react-compiler
  reactCompiler: false,
};

export default nextConfig;
